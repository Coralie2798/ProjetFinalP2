import { CompagnieMaritime } from './compagnie-maritime.model';

describe('CompagnieMaritime', () => {
  it('should create an instance', () => {
    expect(new CompagnieMaritime()).toBeTruthy();
  });
});
